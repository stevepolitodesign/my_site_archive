<% content_for :page_title do %>
    Change Plans
<% end %>
<div class="row row-cols-1 row-cols-md-3 justify-content-center">
    <% @available_plans.each do |plan| %>
        <div class="col mb-4">
            <div class="card border-0 shadow-sm h-100 text-center">
                <div class="card-body d-flex align-items-center flex-column">
                    <h5 class="card-tite"><%= plan.formatted_name %></h5>
                    <ul class="list-group list-group-flush mb-4">
                        <% if plan.interval == "yearly"%>
                            <li class="list-group-item px-0"><strong>Get 2 months free.</strong></li>
                        <% end %>
                        <li class="list-group-item px-0">Monitor up to 10 websites.</li>
                        <li class="list-group-item px-0">Monitor up to 10 webpages per website.</li>
                        <li class="list-group-item px-0">Weekly screenshot generation per webpage.</li>
                        <li class="list-group-item px-0">Weekly source code generation per webpage.</li>
                        <li class="list-group-item px-0">Weekly DNS record generation per website.</li>
                    </ul>
                    <%= form_with url: subscription_path, local: true, method: :put do |f| %>
                        <%= f.hidden_field :plan, value: plan.processor_id %>
                        <%= f.submit "Upgrade to #{plan.formatted_name}", class: "btn btn-sm btn-primary" %>
                    <% end %>
                </div>
            </div>
        </div>    
    <% end %>
</div>